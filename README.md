# PocketLLM
PocketLLM is an end-to-end pipeline for building ultra-small language models that run on microcontrollers such as ESP32. It includes dataset generation, training of tiny LLMs (~20M parameters), quantization, and deployment on resource-constrained edge devices.
